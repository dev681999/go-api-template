package constants

// Layers
const (
	LayerDatabase  string = "database"
	LayerRepo      string = "repo"
	LayerService   string = "service"
	LayerTransport string = "transport"
)

// Services
const (
	SvcUser    = "user"
	SvcStorage = "storage"
)
